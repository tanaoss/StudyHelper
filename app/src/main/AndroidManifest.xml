<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hfut.studyhelper">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 允许应用程序改变网络状态 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" /> <!-- 允许应用程序改变WIFI连接状态 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 允许应用程序访问有关的网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 允许应用程序访问WIFI网卡的网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 允许应用程序完全使用网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScoreActivity"
            android:theme="@style/AppTheme.Main"></activity>
        <activity
            android:name=".configTime"
            android:theme="@style/my_time_dialog_style" />
        <activity
            android:name=".TimeActivity"
            android:theme="@style/AppTheme.Main" />
        <activity
            android:name=".editDiologActiity"
            android:theme="@style/dialog_style" />
        <activity
            android:name=".editActivity"
            android:theme="@style/AppTheme.Main" />
        <activity
            android:name=".schedule_week"
            android:theme="@style/AppTheme.Main" />
        <activity
            android:name=".schedulActivity"
            android:theme="@style/AppTheme.Main" />
        <activity
            android:name=".dialogActivities"
            android:theme="@style/dialog_style" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>